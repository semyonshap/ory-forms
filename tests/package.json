{
  "name": "test",
  "private": true,
  "scripts": {
    "docker:up": "docker compose -f ory/docker-compose.yaml up --build",
    "docker:down": "docker compose -f ory/docker-compose.yaml down",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:headed": "playwright test --headed",
    "e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "testcontainers": "^12.1.0"
  }
}
