{
  "name": "test",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "docker:up": "docker compose -f ory/docker-compose.yaml up --build",
    "e2e:docker:up": "docker compose -f e2e/docker-compose.yaml up --build",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:headed": "playwright test --headed",
    "e2e:install": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0"
  }
}
